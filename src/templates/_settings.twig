{# @var plugin \WelfordMedia\GoingPostal\GoingPostal #}
{# @var settings \WelfordMedia\GoingPostal\models\Settings #}

{% import '_includes/forms.twig' as forms %}

{{ forms.textField({
    label: 'Postal Server',
    instructions: 'The URL or IP address of the Postal server to send mail through. Do not include https:// or a trailing slash.',
    id: 'host',
    suggestEnvVars: true,
    name: 'host',
    value: adapter.host,
    errors: adapter.getErrors('host')
}) }}

{{ forms.textField({
    label: 'API Key',
    instructions: 'The API key to use when communicating with the Postal server.',
    id: 'api-key',
    suggestEnvVars: true,
    name: 'apiKey',
    value: adapter.apiKey,
    errors: adapter.getErrors('apiKey')
}) }}

{{ forms.checkBox({
    label: 'Send Using RAW Messages',
    instructions: 'If enabled, the plugin will send messages using the RAW API endpoint. This is useful if you are using a custom template or need to send attachments.',
    id: 'raw',
    suggestEnvVars: true,
    name: 'useRawMessage',
    value: 1,
    checked: adapter.useRawMessage == 1,
    errors: adapter.getErrors('useRawMessage')
}) }}